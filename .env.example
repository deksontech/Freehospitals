PORT=3000
CONTACT_TO=vishal@deksontech.com
ADMIN_PASSWORD=Red2blue
ADMIN_WHATSAPP_TO=+917838883008

# Optional: add these to send emails through Resend.
# Without them, the backend still saves every request in data/contact-requests.json.
RESEND_API_KEY=
CONTACT_FROM=Freehospitals <onboarding@resend.dev>

# Optional: add these to send a WhatsApp alert to the admin when a new enquiry is submitted.
# Twilio requires an approved WhatsApp sender in whatsapp:+E.164 format.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
